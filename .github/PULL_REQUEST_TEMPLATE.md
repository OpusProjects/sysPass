## Summary

<!-- What does this PR do and why? -->

-

## Test plan

- [ ] Unit tests pass (`composer install && vendor/bin/phpunit --group unitary`)
- [ ] Integration tests pass (`vendor/bin/phpunit --group integration`)
- [ ] `composer phpcs` passes
- [ ] `vendor/bin/phpstan analyse --level 6 src` passes
